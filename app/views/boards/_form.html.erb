<%= form_with model: board, class: 'form-control w-full max-w-sm' do |form| %>
  <% if board.errors.any? %>
    <div id='error_explanation'>
      <h2><%= pluralize(board.errors.count, "error") %> prohibited this board from being saved:</h2>

      <ul>
        <% board.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.text_field :title, class: 'input input-primary', placeholder: 'title' %>
  <%= form.text_field :appname, class: 'input input-primary', placeholder: 'appname' %>
  <%= form.text_field :description, class: 'input input-primary', placeholder: 'description' %>
  <%= form.text_field :website, class: 'input input-primary', placeholder: 'website' %>
  <%= form.submit 'Create Board', class: 'btn btn-primary' %>
<% end %>
