<div class="mx-auto md:w-2/3 w-full flex">
  <div class="mx-auto">
    <% if notice.present? %>
      <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
    <% end %>

    <%= render @board %>

    <%= link_to "Edit this board", edit_portal_board_path(@board), class: "btn btn-primary" %>
    <%= link_to "Back to boards", portal_boards_path, class: "btn" %>
    <div class="inline-block ml-2">
      <%= button_to "Destroy this board", [:portal, @board], method: :delete, class: "btn btn-error" %>
    </div>
  </div>
</div>
